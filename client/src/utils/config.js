const LOCAL_URL = "http://localhost:5000";
const PROD_URL = "https://tahfeeth-system.onrender.com";

// export const API_URL =
//   process.env.NODE_ENV === "development" ? LOCAL_URL : PROD_URL;

export const API_URL = PROD_URL;
